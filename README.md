# leegeo.github.io

静态站点已按“一个 App 一个目录”重组。

当前结构：

- `/`：根入口页，当前保持空白，由外部重定向到具体 App 子目录
- `/yosia`：氧息 App 的完整介绍站点

后续新增 App 时，建议直接新增同级目录，例如：

- `/app-a`
- `/app-b`

每个 App 目录内部独立维护自己的页面、样式、脚本和资源文件，避免跨目录耦合。
